"use client";\n\nimport { useState, useEffect } from "react";\n\nexport default function EnhancedSearch({ onSearch, placeholder = "Search laptops...", suggestions = [] }) {\n  const [query, setQuery] = useState("");\n  const [showSuggestions, setShowSuggestions] = useState(false);\n  const [isLoading, setIsLoading] = useState(false);\n  const [filteredSuggestions, setFilteredSuggestions] = useState([]);\n\n  useEffect(() => {\n    if (query.length > 0 && suggestions.length > 0) {\n      const filtered = suggestions.filter(suggestion => \n        suggestion.toLowerCase().includes(query.toLowerCase())\n      );\n      setFilteredSuggestions(filtered);\n      setShowSuggestions(filtered.length > 0);\n    } else {\n      setShowSuggestions(false);\n    }\n  }, [query, suggestions]);\n\n  const handleSearch = async (searchQuery) => {\n    setIsLoading(true);\n    await onSearch(searchQuery);\n    setIsLoading(false);\n    setShowSuggestions(false);\n  };\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    if (query.trim()) {\n      handleSearch(query.trim());\n    }\n  };\n\n  const handleSuggestionClick = (suggestion) => {\n    setQuery(suggestion);\n    handleSearch(suggestion);\n  };\n\n  const popularSearches = [\n    "Gaming Laptops",\n    "MacBook Pro",\n    "Dell XPS",\n    "ThinkPad",\n    "Budget Laptops",\n    "2-in-1 Laptops"\n  ];\n\n  return (\n    <div className="relative max-w-2xl mx-auto">\n      <form onSubmit={handleSubmit} className="relative">\n        <div className="search-enhanced rounded-2xl overflow-hidden">\n          {/* Search Icon */}\n          <div className="absolute left-4 top-1/2 transform -translate-y-1/2 z-10">\n            {isLoading ? (\n              <div className="w-5 h-5 border-2 border-orange-500 border-t-transparent rounded-full animate-spin"></div>\n            ) : (\n              <svg className="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />\n              </svg>\n            )}\n          </div>\n\n          {/* Search Input */}\n          <input\n            type="text"\n            value={query}\n            onChange={(e) => setQuery(e.target.value)}\n            placeholder={placeholder}\n            className="w-full pl-12 pr-32 py-4 bg-transparent text-white placeholder-gray-400 focus:outline-none text-lg"\n            disabled={isLoading}\n          />\n\n          {/* Voice Search Button */}\n          <button\n            type="button"\n            className="absolute right-20 top-1/2 transform -translate-y-1/2 p-2 text-gray-400 hover:text-orange-500 transition-colors"\n          >\n            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z" />\n            </svg>\n          </button>\n\n          {/* Clear Button */}\n          {query && (\n            <button\n              type="button"\n              onClick={() => setQuery("")}\n              className="absolute right-12 top-1/2 transform -translate-y-1/2 p-1 text-gray-400 hover:text-white transition-colors"\n            >\n              <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />\n              </svg>\n            </button>\n          )}\n\n          {/* Search Button */}\n          <button\n            type="submit"\n            disabled={!query.trim() || isLoading}\n            className="absolute right-2 top-1/2 transform -translate-y-1/2 bg-orange-500 hover:bg-orange-600 disabled:bg-gray-600 disabled:cursor-not-allowed text-white px-4 py-2 rounded-xl transition-colors font-medium"\n          >\n            {isLoading ? "..." : "Search"}\n          </button>\n        </div>\n      </form>\n\n      {/* Suggestions Dropdown */}\n      {showSuggestions && filteredSuggestions.length > 0 && (\n        <div className="absolute top-full left-0 right-0 mt-2 bg-gray-900 border border-gray-700 rounded-xl shadow-xl z-50 max-h-60 overflow-y-auto">\n          <div className="p-2">\n            <div className="text-xs text-gray-400 uppercase tracking-wider mb-2 px-3">Suggestions</div>\n            {filteredSuggestions.slice(0, 6).map((suggestion, index) => (\n              <button\n                key={index}\n                onClick={() => handleSuggestionClick(suggestion)}\n                className="w-full text-left px-3 py-2 hover:bg-gray-800 rounded-lg transition-colors text-gray-300 hover:text-white flex items-center gap-3"\n              >\n                <svg className="w-4 h-4 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />\n                </svg>\n                <span>{suggestion}</span>\n              </button>\n            ))}\n          </div>\n        </div>\n      )}\n\n      {/* Popular Searches */}\n      {!query && !showSuggestions && (\n        <div className="mt-6 text-center animate-fadeIn">\n          <p className="text-gray-400 text-sm mb-3">Popular searches:</p>\n          <div className="flex flex-wrap gap-2 justify-center">\n            {popularSearches.map((search, index) => (\n              <button\n                key={index}\n                onClick={() => handleSuggestionClick(search)}\n                className="px-3 py-1 bg-gray-800 hover:bg-orange-500 text-gray-300 hover:text-white rounded-full text-sm transition-all duration-300 hover:scale-105"\n              >\n                {search}\n              </button>\n            ))}\n          </div>\n        </div>\n      )}\n\n      {/* Search Tips */}\n      {query.length > 0 && filteredSuggestions.length === 0 && (\n        <div className="mt-4 p-4 bg-gray-900 rounded-xl border border-gray-700">\n          <div className="flex items-start gap-3">\n            <svg className="w-5 h-5 text-orange-500 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />\n            </svg>\n            <div>\n              <h4 className="font-medium text-white mb-2">Search Tips</h4>\n              <ul className="text-sm text-gray-400 space-y-1">\n                <li>• Try using brand names like "MacBook", "ThinkPad", or "Dell"</li>\n                <li>• Use descriptive terms like "gaming", "ultrabook", or "2-in-1"</li>\n                <li>• Check spelling and try different variations</li>\n              </ul>\n            </div>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n}\n\nexport function QuickFilters({ onFilterChange, activeFilters = {} }) {\n  const filters = [\n    { key: "brand", label: "Brand", options: ["Apple", "Dell", "HP", "Lenovo", "ASUS", "Acer"] },\n    { key: "priceRange", label: "Price", options: ["Under $500", "$500-$1000", "$1000-$2000", "Over $2000"] },\n    { key: "type", label: "Type", options: ["Gaming", "Business", "Ultrabook", "2-in-1", "Student"] },\n    { key: "processor", label: "Processor", options: ["Intel i3", "Intel i5", "Intel i7", "AMD Ryzen"] }\n  ];\n\n  return (\n    <div className="flex flex-wrap gap-4 justify-center animate-slideIn">\n      {filters.map((filter) => (\n        <div key={filter.key} className="relative">\n          <select\n            onChange={(e) => onFilterChange(filter.key, e.target.value)}\n            value={activeFilters[filter.key] || ""}\n            className="appearance-none bg-gray-800 text-white px-4 py-2 pr-8 rounded-xl border border-gray-600 focus:border-orange-500 focus:outline-none transition-colors"\n          >\n            <option value="">{filter.label}</option>\n            {filter.options.map((option) => (\n              <option key={option} value={option}>\n                {option}\n              </option>\n            ))}\n          </select>\n          <svg className="absolute right-2 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400 pointer-events-none" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />\n          </svg>\n        </div>\n      ))}\n      \n      {Object.keys(activeFilters).length > 0 && (\n        <button\n          onClick={() => onFilterChange("clear", "")}\n          className="px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-xl transition-colors flex items-center gap-2"\n        >\n          <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">\n            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />\n          </svg>\n          Clear All\n        </button>\n      )}\n    </div>\n  );\n}\n